apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-devsecops.fullname" . }}-test-connection"
  labels:
    {{- include "helm-devsecops.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-devsecops.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
